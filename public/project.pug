doctype html
html
	head
		meta(charset="utf-8")
		meta(name="viewport", content="width=device-width")
		link(rel="stylesheet", href="../style.css")
		title Alexsol.is
	body
		header
			h1.header AlexSol
				span.orange .is
			nav
				a(href='../../') Home
		main
			article
				h1= project.displayName
				p= project.description
				a(href=project.url) See Original ↗
			iframe(src=project.url)